import React from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render(
  <React.StrictMode>
    <div>This module has not been fully developed. Only the addon is currently available.</div>
  </React.StrictMode>,
  document.getElementById('root')
);
